Summary:
   Initial installation

setenv OBITBASE /export/ssd/bcotton/Git/ObitBase
setenv GitObitBase /export/raid_2/bcotton/ObitGitMaster/ObitBase
cd $OBITBASE; /bin/cp -p  README Makefile doDistro.py $GitObitBase
cd $GitObitBase ; git add README Makefile doDistro.py
cd $GitObitBase ; git add SETUP.deps SETUP.Obit SETUP.ObitTalk SETUP.ObitView
cd $GitObitBase/bin ; git add Makefile
cd $GitObitBase/copyFiles ; git add readme dot.obitrc_bin.py dot.obitrc.py ObitTalk ObitTalk3
cd $GitObitBase/FITS ; git add 3C196_SBand.fits.gz
cd $GitObitBase/scripts ; git add build_with_SSE build_with_AVX build_with_AVX2 build_with_AVX512
cd $GitObitBase/scripts ; git add rebuild_with_SSE rebuild_with_AVX rebuild_with_AVX2 rebuild_with_AVX512
cd $GitObitBase/share ; git add share.tgz
cd $GitObitBase/template ; git add template.tgz
cd $GitObitBase/deps/src/cfitsio ; git add *
cd $GitObitBase/deps/src/plplot ; git add *
cd $GitObitBase/deps/src/curl ; git add *
cd $GitObitBase/deps/src/xml2 ; git add *
cd $GitObitBase/deps/src/xmlrpc ; git add *
31 Mar 2025

Summary:
   

setenv OBITBASE /export/ssd/bcotton/Git/ObitBase
setenv GitObitBase /export/raid_2/bcotton/ObitGitMaster/ObitBase
cd $OBITBASE; /bin/cp -p .gitignore  $GitObitBase
cd $GitObitBase ; git add .gitignore 
31 Mar 2025

Summary:  Update description of libraries needed
cd $OBITBASE; /bin/cp -p changes.text $GitObitBase
cd $OBITBASE; git add changes.text 
cd $OBITBASE; /bin/cp -p README LibrariesNeeded.text $GitObitBase
cd $OBITBASE ; git add README LibrariesNeeded.text
01 Jun 2025

# Maintain github repository directly out of $OBITBASE
# but maintain a copy in $GitObitBase
cd $OBITBASE; /bin/cp -p changes.text $GitObitBase
cd $OBITBASE; git add changes.text 
#cd $OBITBASE; /bin/cp -p  $GitObitBase
#cd $OBITBASE; git add

# Edit $OBITBASE/changes
  
# Push commits to github
cd $OBITBASE
git commit -F changes
git push -v ObitBase master
# May have to force: git push -v -f ObitBase master

# Update local version
cd $OBITBASE; git pull origin master

# To create a local copy
git clone https://github.com/bill-cotton/ObitBase/
  
13 June 2025
Summary:  Update Obit Version
cd $OBITBASE; git add changes.text 
cd $OBITBASE; git add Makefile copyFiles/readme copyFiles/dot.obitrc.py copyFiles/dot.obitrc_bin.py 
cd $OBITBASE;
# Update changes
git commit -F changes
git push -v ObitBase master
# Update local version
cd $OBITBASE; git pull origin master


07 July 2025
Summary: Add -lm to tasks/Makefile.in
cd $OBITBASE; git add changes.text 
cd $OBITBASE; git add src/Obit/tasks/Makefile.in
# Update changes
git commit -F changes
git push -v ObitBase master
# Update local version
cd $OBITBASE; git pull origin master


07 July 2025
Summary: Add -lm to tasks/Makefile.in
cd $OBITBASE; git add changes.text 
cd $OBITBASE; git add src/Obit/tasks/Makefile.in
# Update changes
git commit -F changes
git push -v ObitBase master
# Update local version
cd $OBITBASE; git pull origin master

18 Aug 2025
Summary: Hacks for ubunto linker woes, sincos fixup, RHEL9 fixes
  Makefile, SETUP.Obit, SETUP.ObitView,
  fixed/configure.Obit fixed/configure.ObitView 
    Ubuntu hacks, special configure scripts
  fixed/autoconfig.stuff.tgz
    modified filed needed to make configure
  fixed/Makefile.in.ObitView.task fixed/Makefile.in.ObitView.src
  fixed/Makefile.in.Obit.tasks
    modified Makefiles
  src/Obit/configure.ac src/Obit/tasks/Makefile.in
  src/Obit/m4/cfitsio.m4 src/Obit/m4/gsl.m4 src/Obit/m4/plplot.m4 src/Obit/m4/python3.m4  src/Obit/m4/xmlrpc.m4
  src/ObitView/configure.ac src/ObitView/Makefile.in src/ObitView/src/Makefile.in
  src/ObitView/m4/xmlrpc.m4 src/ObitView/m4/obit.m4 
    Ubuntu hacks packaged into fixed/autoconfig.stuff.tgz
  copyFiles/readme copyFiles/dot.obitrc.py copyFiles/dot.obitrc_bin.py
    Update ObitVersion

# Gandalf  SHIT - this Must be maintained from smeagle - no git installation here.
setenv OBITBASE /export/home/gandalf/bcotton/ssd/bcotton/Git/ObitBase
setenv GitObitBase /export/raid/bcotton/ObitGitMaster/ObitBase
# Smeagle
setenv OBITBASE /export/ssd/bcotton/ssd/Git/ObitBase
setenv GitObitBase /export/raid_2/bcotton/ObitGitMaster/ObitBase

# Maintain github repository directly out of $OBITBASE
# but maintain a copy in $GitObitBase
cd $OBITBASE; /bin/cp -p Makefile SETUP.Obit SETUP.ObitView changes.text $GitObitBase
git add SETUP.Obit SETUP.ObitView changes.text
cd $OBITBASE/fixed/; /bin/cp -p configure.Obit configure.ObitView autoconfig.stuff.tgz $GitObitBase/fixed/
git add  configure.Obit configure.ObitView autoconfig.stuff.tgz
cd $OBITBASE/fixed/; /bin/cp -p Makefile.in.ObitView.tasks Makefile.in.ObitView.src $GitObitBase/fixed/
git add  Makefile.in.ObitView.tasks Makefile.in.ObitView.src 
cd $OBITBASE/fixed/; /bin/cp -p Makefile.in.Obit.tasks  $GitObitBase/fixed/
git add Makefile.in.Obit.tasks
cd $OBITBASE/copyfiles; /bin/cp -p readme dot.obitrc.py dot.obitrc_bin.py $GitObitBase/copyfiles 
git add readmedot.obitrc.py dot.obitrc_bin.py 

###### RHEL9  Jan 2026
Summary: bug fixes
   template/bin/Basic
     Fix OBITEXEC

setenv OBITBASE /export/home/smeagle/bcotton/Git/ObitBase
setenv GitObitBase /export/raid_2/bcotton/ObitGitMaster/ObitBase

cd $OBITBASE; /bin/cp -p template/bin/Basic changes.text $GitObitBase
git add template/bin/Basic changes.text

#Update changes.text
cd $OBITBASE
git commit -F changes.text
git push -v ObitBase master

